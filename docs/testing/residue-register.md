# Residue register

The single place that says what is open.

Cluster `C6` of [`residue-stream-2026-08-15.md`](residue-stream-2026-08-15.md) is that no document
answers that question: closure is recorded in prose — *"`LAF-28` closed"* in a run log, a sentence in
a release paragraph — so a cross-reference over all 58 `LAF-*` findings classifies 50 of them as *no
closure statement found*, which is not the truth. Items are then re-discovered rather than resolved.

This file exists so the answer is a lookup. It is enforced by `scripts/docs_check.py`, which fails
when a document names a finding this file does not carry, and when a document lists as *shipped open*
a finding this file records as `closed`. Documents may describe a finding at any length; they may not
disagree with this table about its state.

## Scope

**Seeded from the twenty-eight items of the `2026-08-15` stream, plus what implementing the response
to it found — not from all 58.** A register that claimed to cover a history it cannot reconstruct
would be the same defect one level up. Findings older than the stream are outside it, and a row is
added here the first time an older id is referred to again.

## Dispositions

| Value | Means |
|---|---|
| `open` | true today, and nothing in the shipped code addresses it |
| `closed` | no longer true, with the reproduction that establishes it in the last column |
| `visible` | still true, and now observable — reported by a command rather than repaired |
| `deferred` | out of scope by an explicit decision recorded in a design, not by neglect |

`visible` and `deferred` are *not* `closed`. A document may list either as shipped open.

## Checked documents

These are the documents `docs_check` requires to agree with the table below, in **both** directions —
a finding listed under a *shipped open* heading in one of them must not be `closed` here (`DOC009`),
and a disposition one of them claims must be the disposition recorded here (`DOC010`):

- checked: `docs/plan/*.md`
- checked: `docs/design/*.md`
- checked: `docs/release/compatibility-v14.md`
- checked: `docs/release/release-checklist-v14.md`

**How a document states a disposition.** In one table cell, holding one word in backticks and
nothing else, beside the finding it is about — the shape `compatibility-v14.md` already uses:

| Finding | Now | Established by |
|---|---|---|
| `LAF-99` — the example | `closed` | what establishes it |

That cell is a claim about today and `DOC010` checks it. A sentence *about* a finding is not a
claim in this sense and is not read: **`LAF-83`** is the residue of that decision, and the reason
for it is that deciding a disposition by reading prose is the thing this register was written to
stop documents doing.

**A release note is checked until it is published, and never after.** `github-release-v2.6.0.md` was
in this list while it was still the text about to be pasted into a release — a current document
making current claims. It was published on `2026-08-15` and the entry came out the same day, which is
the rule working rather than the rule being abandoned. `LAF-74` is what putting it here found, and
the next release note joins the list the moment it is written and leaves it the moment it ships.

**Released documents are deliberately absent.** `github-release-v2.5.0.md` and
`release-checklist-v13.md` list `LAF-52`..`LAF-59` as shipped open, and that was true when they
shipped. Editing them to agree with today would destroy the evidence they exist to be. A dated record
does not have to agree with the present; a current document does.

## Register

| ID | Severity | Found in | Disposition | Closed or made visible by |
|---|---|---|---|---|
| `LAF-43` | medium | `2.4.0` live acceptance | `deferred` | — cluster C5; `DESIGN-readable-receipt.md` §5 leaves it for its own stream. **Re-checked `2026-08-16`, and it holds twice over** (`LA-S-17`, `LAS-72`). Live against a locally built `2.6.0` wheel: `source add` with `file:///…/repo` and with a plain absolute path to a real Git repository is refused for both `source-git` and `registry-git`, all three exits `1`. In code the refusal has **two layers, not one**: `configuration/schema.py:201` rejects the location as *not a safe credential-free Git location* before the transport check at `sources/git.py:198` is ever reached. And the escape hatch is dead — `GitSnapshotRequest.allow_local_transport` defaults `False` (`sources/model.py:362`) and **all three construction sites in the package leave the default**; `curation/runtime.py:241` even passes `False` explicitly. Only `tests/git_source_adapter_test.py` and `tests/curation_runtime_test.py` set it `True`. So a local upstream cannot be rehearsed by any flag, and the remedy is two changes rather than one |
| `LAF-45` | medium | `2.4.0` live acceptance | `closed` | `registry audit --check-upstream` ends with one `info` line — *checked 1 vendored artifact against its upstream: 1 up-to-date, 0 changed, 0 unreachable*, or *no vendored artifacts to check against upstream* — and prints nothing of the kind without the flag. `tests/registry_vendor_license_test.py::test_laf45_*` cover all four shapes through the real CLI; live `LA-R-31`/`LA-R-32` in [`PROGRESS-live-acceptance-v8.md`](PROGRESS-live-acceptance-v8.md), where the `main` wheel's output with and without the flag is byte-identical and the branch's differs by that line |
| `LAF-47` | medium | `2.4.0` live acceptance | `open` | — see *Corrections* below |
| `LAF-49` | low | `2.4.0` live acceptance | `closed` | `docs/configuration/git-environment-v1.md` names what the Git subprocess receives, what it does not, and the `git config --global http.proxy` route that works because `HOME` is passed; the README points at it from *Maintaining a registry*. The behaviour is unchanged and deliberate — this row was always a documentation gap. `tests/git_environment_docs_test.py` reads the page's three tables and compares them against `_ALLOWED_ENVIRONMENT` and the real `_safe_environment`, so a variable added to the code and not to the page fails the suite |
| `LAF-52` | high | `2.5.0` live acceptance | `closed` | `RR-2A`; `tests/setup_render_test.py`, and `marketplace setup` at a terminal prints the failure detail, the artifact key and the manual route |
| `LAF-53` | high | `2.5.0` live acceptance | `closed` | `RR-4`; `aart marketplace receipt undo <coordinate>` |
| `LAF-54` | high | `2.5.0` live acceptance | `closed` | `RR-2A`; `marketplace setup` without `--yes` prints the effect list, the capabilities and the manual alternative before asking for approval |
| `LAF-55` | high | `2.5.0` live acceptance | `closed` | `RR-3`; `receipt verify` asks the Keychain whether the item holds a non-empty value, `tests/setup_verify_test.py` |
| `LAF-57` | low | `2.5.0` live acceptance | `open` | — still true, and **one of its four stated causes stops being true the moment `RS-12` merges.** The row's explanation is mode `0600` vs `0644`, a per-build mtime, the hand build's inherited buildx defaults, and *AART's build running without `HOME`*. On `fix/setup-docker-credentials-rs12`, `_docker_env` gives docker steps `HOME` and `DOCKER_CONFIG` (`setup_runtime.py:165`), so the last cause is gone and the attestation-manifest difference it produced may go with it. The first three are untouched, so the finding stands; its explanation needs re-measuring after that merge. Re-read `2026-08-16` |
| `LAF-58` | medium | `2.5.0` live acceptance | `open` | — `RR-4` names the limit in the undo review before consent; closing it needs `RR-4A` at the capture site. Still true, and the capture site is closer to it than the row suggests: `setup_runtime.py:451` already runs `docker image inspect <tag>` *before* the build and keeps only `returncode == 0` as `preexisting`, while `:463` runs `docker image inspect --format {{.Id}}` *after* it. The earlier binding is one flag on a call the code already makes. The undo review names the limit and cites `LAF-58` by id (`setup_undo.py:153`). Re-read `2026-08-16` |
| `LAF-59` | high | `2.5.0` live acceptance | `closed` | `RR-2B`; a build failing on its last instruction reports that instruction and its exit code |
| `LAF-61` | medium | `2.5.0` live acceptance | `visible` | `RR-10D`; `receipt verify` names an orphaned run directory and removes nothing. Both halves confirmed live on `2026-08-16` against a locally built `2.6.0` wheel: a working copy planted under the data root was reported by `verify` with its full path, and it is still on disk afterwards with its contents intact. `visible` is the right disposition and it is now measured, not argued. Claimed `visible` once already on a probe that read the wrong root (`LAF-66`); the claim is made again on a test that drives the real writer and the real reader together |
| `LAF-62` | medium | `2.5.0` publication | `deferred` | — cluster C4; needs the index-version boundary stream. **The disposition disagrees with the commit that filed the finding.** [`6589778`](https://github.com/M1F1/agent-artifacts/commit/6589778) measured it and says *Recorded as `LAF-62` and left open*, and the only design that names it is `DESIGN-readable-receipt.md` §5, which records that *that* design does not touch it — a scope exclusion, not a decision about the boundary. The register's own definition of `deferred` asks for a decision recorded in a design. Left `deferred` and not flipped: the call is a judgement, not a failed reproduction. The disagreement is `LAF-99`. Re-read `2026-08-16` |
| `LAF-63` | high | implementing `RR-2A` | `closed` | `RR-10A`; one redactor in `agent_artifacts/redaction.py`, matching a credential name with any prefix, `tests/setup_render_test.py::test_laf63_a_prefixed_credential_name_is_redacted` |
| `LAF-64` | medium | implementing `RR-5` | `closed` | `_curses_install_scope_event` always answers with a `WizardInput`; the `wizard` flag and the scope-only return are both gone, and with them the eleven lines of shape-branching at each call site. `tests/tui_install_scope_test.py` drives the real selector with a scripted screen for `confirm`/`back`/`quit`, and two guards keep the trap from returning: the signature carries no mode flag, and `_curses_install_scope` no longer exists to be called by accident |
| `RS-01` | medium | `2.3.0` prose | `closed` | `registry audit` runs the delivery check for every artifact package it walks, not only inside the vendored branch; `tests/registry_vendor_delivery_test.py::OwnedMcpDeliveryTest`, and live acceptance v10 `LA-R-34`/`LA-R-35` on a registry authored entirely by the `main` wheel. `registry validate` is unchanged on purpose (`LA-R-36`): it is also the consumer's gate |
| `RS-02` | low | `2.3.0` prose | `open` | — |
| `RS-03` | medium | `2.3.0` prose | `deferred` | — cluster C5, with `LAF-43`. **Re-checked `2026-08-16`; still true, and the two channels say it differently** (`LA-S-18`, `LAS-73`). Live: a `source-local` tree with one symlink under an artifact payload is refused with *local source symlinks are forbidden: artifacts/skill/demo/payload/alias.md* — the whole acquisition, not that entry, and there is no flag to allow it (`sources/local.py:68`). The Git channel refuses the same shape at `sources/git.py:101`, which accepts only modes `100644` and `100755` and calls anything else *Git tree contains an unsafe entry* — the same rule, a message that does not say the word symlink. Which of the two an operator meets is decided by `LAF-43`: the Git channel cannot be reached locally at all, so the message a vendoring would show is the one nobody can see. The knot C5 names is real and it is this |
| `RS-04` | low | `2.3.0` prose | `open` | — |
| `RS-05` | low | `2.3.0` prose | `open` | — still true, and one degree worse than it reads. Nothing imports `agent_artifacts/io/cache.py` (94 lines, *Immutable snapshot cache — shell (WP-7)*); the only `cache` imports in the package are `io/security_cache.py` and `security/cache.py`, which are different modules. It is also **in the published wheel** — `agent_artifacts/io/cache.py` is present in `agent_artifacts-2.6.0-py3-none-any.whl`, so the dead module ships. Re-read `2026-08-16` |
| `RS-06` | low | `2.3.0` prose | `open` | — still true. `docs/design/DESIGN-upstream.md` contains no occurrence of *superseded*, *obsolete* or *replaced by*, and its opening still presents itself as a live companion to `DESIGN.md`. Re-read `2026-08-16` |
| `RS-07` | medium | `2.2.0` prose | `open` | — |
| `RS-08` | medium | `2.2.0` prose | `closed` | a root `aart-registry.json` that is present must be a regular file that parses, or the subscription is refused — the decision the `2.2.0` plan said needed taking is recorded in `DESIGN-subscription-identity-binding.md` §2. `tests/identity_agreement_test.py::test_rs08_*` cover the three broken shapes and the healthy one; live `LA-S-14`..`LA-S-16` in [`PROGRESS-live-acceptance-v9.md`](PROGRESS-live-acceptance-v9.md), where the `main` wheel admits the source, installs from it, and writes an uncorroborated `declared_id` into the consumer's manifest |
| `RS-09` | medium | `2.2.0` prose | `closed` | every registry refusal and every `validate`/`audit` report finding carries a next step, from a shared vocabulary in `registry_commands/planning.py` and `commands/registry.py`; `_emit_report` prints remediation in text as the JSON envelope always did. `tests/source_remediation_test.py::RegistryRefusalRemediationTest` reads the syntax of both shipped modules, so a refusal or finding added later without one fails, and it drives `registry audit` and `registry test` through the CLI. Reproduction: `aart registry validate --strict` on a registry whose lock is missing prints two errors, each with the command that fixes it |
| `RS-10` | medium | `2.2.0` prose | `open` | — see *Corrections* below |
| `RS-11` | low | `2.5.0` prose | `open` | — still true. `setup.py:584`: `if entry["type"] != "secret": raise _Invalid("inputs[N].type must be 'secret'")`. A recipe still cannot prompt for a username. Re-read `2026-08-16` |
| `RS-12` | medium | `2.5.0` prose | `closed` | `_docker_env` gives both docker adapters — and the `docker.build@1` rollback — `HOME` and `DOCKER_CONFIG`, derived from `HOME` when not set explicitly; `tests/setup_docker_credentials_test.py` drives the real adapters through `apply_setup_plan` and asserts the pull's environment, that the keychain step gains neither name, and that a failed pull now reports docker's own words. Walked live in [`PROGRESS-live-acceptance-v4.md`](PROGRESS-live-acceptance-v4.md) against two real executables one commit apart: a sandbox `config.json` naming a credential helper that does not exist fails the run with the fix and is invisible without it (`LA-M-08a`). A pull that authenticates against a real private registry stays human-gated (`LA-M-10`) |
| `RS-13` | low | `2.5.0` prose | `open` | — still true. The shell modules are `shell.env-from-keychain@1` and `file.managed-block@1`; there is no `shell.zshrc-managed-block@1` in the module table at `setup.py:64`. Re-read `2026-08-16` |
| `RS-14` | low | `2.5.0` prose | `open` | — still true, and it is the strictness that does it: `setup.py:389` allows a step exactly `{id, use, with}` and refuses anything else with *step has unknown field(s)*. `_comment` appears nowhere in the package. Re-read `2026-08-16` |
| `RS-15` | low | `2.5.0` prose | `open` | — still true. `native_tree.py:451` fixes the package root to `artifact.json`, `README.md`, `SETUP.md`, `provenance.json`, `payload`, `setup`; anything else is *unexpected canonical package path*. A script can live under `payload/` or `setup/`, which is the route the row does not mention. Re-read `2026-08-16` |
| `LAF-65` | medium | `2.6.0` live acceptance | `closed` | `RR-10E`; `rollback_command` names `receipt undo`, and `tests/setup_custom_test.py::WrittenCommandFieldTests` hands the written field to the shipped CLI parser so it cannot go stale again |
| `LAF-66` | high | `2.6.0` live acceptance | `closed` | `RR-10D`; the probe takes the run root the engine writes into, answers `unknown` when it has no root to read, and `tests/setup_verify_test.py::test_laf66_the_probe_reads_the_root_the_engine_writes_into` drives the real writer and the real reader together |
| `LAF-67` | medium | `2.6.0` live acceptance | `open` | — still true, re-measured against the live registries rather than the walk. Registry A publishes three setup recipes — `mcp/github-docker`, `mcp/github-enterprise-docker`, `mcp/postgres-docker` — and every one of them uses `docker.pull@1`; Registry B publishes none. No published artifact uses `docker.build@1`, so the two acceptance criteria that need one are still unreachable against published content. Re-read `2026-08-16` |
| `LAF-68` | medium | `2.6.0` live acceptance | `closed` | — [#1](https://github.com/M1F1/agent-artifacts-live-acceptance-project/pull/1) merged: the acceptance runner is on `2.6.0` and reconciles eleven installations against the published wheel, so `main` no longer pins a runner three releases behind the tool it exercises |
| `LAF-69` | high | using this register | `closed` | `DOC010` fails a checked document whose disposition cell disagrees with this table, so the gate now runs both ways. Reproduction: flip `LAF-61` here to `open` — the state it was actually in — and `docs_check` names `compatibility-v14.md:160`, the row that stayed wrong for a release while `docs-check` passed. `tests/quality_gates_test.py::ResidueRegisterGateTest`, one test per direction. The prose half of the original case is carved out as `LAF-83` |
| `LAF-70` | medium | triaging for `2.6.0` | `closed` | — the authoring machine runs `2.6.0`, installed from the published release asset after verifying its digest against `wheel-digest`. Registry A's CI gates at `2.5.0` and its pin move is the remaining half, tracked as `LAF-71` |
| `LAF-71` | medium | triaging for `2.6.0` | `open` | — **this row said `closed` and its evidence does not reproduce.** Two of the three moves are real: the acceptance repository [#1](https://github.com/M1F1/agent-artifacts-live-acceptance-project/pull/1) installs `2.6.0` from the published wheel, and Registry A's three workflows [#10](https://github.com/M1F1/agent-artifacts-registry/pull/10) pin `v2.6.0` on `main`. Registry B does not: all three of its workflows pin `v2.5.0`, because [#5](https://github.com/M1F1/agent-artifacts-registry-2/pull/5) moved them from `v2.0.0` to `v2.5.0` while being titled *Move the CI pin to 2.6.0*. Re-checked `2026-08-16`; the mis-read is `LAF-92` |
| `LAF-75` | high | publishing `2.6.0` | `visible` | — `build_wheel.py` builds an unstamped wheel and `wheel-digest` prints the digest of a stamped one; the checklist named them on consecutive lines. Corrected in `release-checklist-v14.md` with the verification step; the real fix is `wheel-digest` emitting the artifact it hashes |
| `LAF-74` | high | publishing `2.6.0` | `closed` | `docs/release/github-release-v2.6.0.md` added to the checked list while it is unpublished; `DOC009` then failed it for `LAF-63`, which is how the stale claim was found |
| `LAF-73` | medium | `2.6.0` live acceptance, second pass | `closed` | `receipt verify` asks whether the recorded `rollback_command` is a command this executable accepts, and when it is not, names the one that is — reported, never rewritten. `tests/setup_verify_test.py::test_laf73_*` drive the real writer (`rollback_command`) and the real reader (the shipped parser) together; live `LA-M-12`..`LA-M-15` in [`PROGRESS-live-acceptance-v6.md`](PROGRESS-live-acceptance-v6.md), where the aged record makes `main` and the branch disagree by exactly one claim and the named command really does reverse the setup |
| `LAF-72` | high | measuring `LAF-63` | `closed` | `RR-10A`, `RR-10C`; there is one `redact_text` and `tests/token_containment_test.py` walks every string of the persisted record, so a field added later is covered without being named |
| `LAF-121` | medium | auditing this run's nine live-acceptance records | `open` | — **seven of the nine run headers written tonight pin the run to a branch name instead of a commit**, so the record stops being re-derivable the moment the branch moves or is deleted. `v6`, `v7`, `v8`, `v9`, `v10`, `v11` and `v12` all write *AART commit under test* as `fix/…`; only `v4` (`87b7fbb`) and `v5` (`1c659a3`) name a commit. `DESIGN-live-acceptance-v1.md`'s header rule asks for a tag or commit, and a branch is neither. Two of the seven also omit the wheel size — `v6` and `v7` — leaving the digest as the only handle on the artifact. Measured `2026-08-16` by re-deriving both: a wheel built from `eedc4a0` in a detached worktree is **543 422 bytes, sha256 `3674e1c9…`**, and from `ac12a6a` **542 418 bytes, `941fae3d…`** — the digests both run headers record, byte for byte. So the records are true today, and they are true only while the branches survive. `LAF-103` is the same defect one degree worse, on `-receipt.md`. Severity `medium`: nothing is wrong in the runs, the loss is the ability to prove that later |
| `LAF-120` | medium | auditing the scenario map across branches | `open` | — the map's stable ids are allocated from whichever copy of the file a branch happens to hold, so two scenarios can be born with the same id and nothing notices. Measured `2026-08-16`: the map on `main` has 113 rows, the one on the run's document chain 138, and **40 ids exist on some branch and on no other** — `LA-0-07`..`LA-0-10`, `LA-R-31`..`LA-R-41`, `LA-S-13`..`LA-S-16`, `LA-U-31`..`LA-U-35`, `LA-M-08`..`LA-M-15`, `LAS-31`..`LAS-33`, `LAS-57`..`LAS-61`. **The collision is not hypothetical — it happened, and it was mine.** `fix/status-names-the-missing-source-rs07` appended `LA-S-11` (*the project is still readable after the last subscription goes*) and `LA-S-12` at `04:12`; ten hours later `docs/recheck-c5-deferred` appended a local-upstream refusal and a symlink refusal under the same two ids, because the chain's copy of the map stops at `LA-S-10`. Corrected on `docs/scenario-id-collision` by renumbering the newer pair to `LA-S-17` and `LA-S-18`, past `LA-S-16` which `fix/broken-registry-descriptor-fails-rs08` already holds; the older rows keep their ids, and no id changed meaning. The file's own rule — append-only, *a correction is `LA-U-14a`, never an edit to `LA-U-14`* — assumes ids are unique, and nothing enforces it: `docs_check.py` reads `docs/plan`, `docs/design`, two released documents and the stream, never the map. Severity `medium`: a repeated id makes a walk unciteable, which is the one thing a stable id is for |
| `LAF-119` | medium | simulating the morning's merge | `open` | — resolving this run's fourteen `fix/` branches the way `PROGRESS.md` told you to leaves the register answering *is it fixed?* twice, differently, for eleven findings. Measured `2026-08-16` with `git merge-tree --write-tree` chained by `git commit-tree`, no branch touched: merging the fourteen into `main` in sequence conflicts **13 times**, and taking both sides of every conflict yields duplicate rows for `LAF-45`, `LAF-47`, `LAF-49`, `LAF-64`, `LAF-73`, `RS-01`, `RS-02`, `RS-07`, `RS-08`, `RS-09` and `RS-10`, each pair holding one `open` row and one `closed` row. Worse, the duplication does not need a conflict: **four of the eight branches whose register merges cleanly still duplicate a row** — `LAF-79`, `LAF-82`, `LAF-83`, `LAF-84`, `LAF-85`, `LAF-87` — because the branch filed its own finding as a row and `docs/register-the-missing-fifteen` later wrote the same id from `PROGRESS.md`; the rows sit far apart and Git joins them silently. Nothing catches it: `docs_check.py` does not read this file (`LAF-100`), and duplicate ids are `LAF-101`, seen here from the merge direction. The instruction is corrected in `PROGRESS.md` to resolve one row per id. The blast radius is also wider than that paragraph said — `live-acceptance-scenarios.md` conflicts 8 times and `registry_commands/planning.py` twice, where `RS-02`, `RS-09` and `RS-04` rewrite the same refusals. Severity `medium`: nothing is lost, but the file that says what is open would say both |
| `LAF-118` | low | walking the config merge on `main` | `open` | — the ownership gate depends on which effect an artifact uses, and only one of the two warns. Measured `2026-08-16` on the wheel built at this commit (`LA-U-40`, `LAS-71`). A `memory` artifact, effect `managed-block`, **refuses** to install when `CLAUDE.md` already exists and is not AART's: *install destinations contain unowned or drifted content; use force*. An `mcp` artifact, effect `merge-json`, installs into an operator-written `.mcp.json` **first try, exit `0`, reporting `changed`**, with no ownership complaint and no `--force`. Both round trips are safe — the uninstall restored the operator's `{"mcpServers":{"mine":…}}` byte for byte — so nothing is lost and the difference may well be deliberate: a merge owns a key, a managed block owns a file. What is not deliberate is that an operator is warned in one case and not the other by the same command, and no document says which effects gate and which do not. Severity `low` |
| `LAF-117` | medium | measuring how far `LAF-116` reaches | `open` | — **removing a source does not remove its content.** Measured `2026-08-16` on the wheel built at this commit, fresh sandbox `HOME` (`LA-U-38`, `LAS-69`). `source add` a `source-local` source, `marketplace status --profile claude` once — which deposits two objects, `LAF-116` — then `aart source remove --alias lab --yes`, which exits `0` saying *source removed: lab; snapshot discarded*. It is: `sources/` is empty and `config.json` lists no sources, so `marketplace list` now refuses with *no source configured*. **The two objects and their six files stay, 24 KB of a source the operator has just removed**, with no reference index, no snapshot that names them and no command that lists them — `object_status`, the projection that would show durable object state, is one of `LAF-106`'s unreachable eight. So *removed* means removed from the configuration and not from the disk, and the difference is invisible from the CLI. Nothing here is unsafe; it is content retained after the operator said to stop using its origin, which on a shared or corporate machine is a question someone will ask |
| `LAF-116` | medium | watching the disk while the commands run | `open` | — **the review that says it changes nothing writes to the durable store, and nothing can take it back.** Measured `2026-08-16` on the wheel built at this commit, a fresh sandbox `HOME`, a `source-local` source and the two-artifact collection from the `RS-08` lab (`LA-U-36`, `LA-U-37`, `LAS-67`, `LAS-68`). After `source add` the object store is empty. `aart marketplace install --profile claude lab/collection/essentials` **without** `--yes` — whose own help says *Without `--yes` the command prints the reviewed plan and exits without changing anything* — exits `0` printing *Reviewed only; re-run with `--yes` to apply this exact plan*, and leaves **six files in two object directories under `objects/sha256/`**. No `state/object-references.json` is written, so those objects are unreferenced from the moment they exist. A second measurement makes it worse: `marketplace install --profile nosuch --yes`, refused with *selected harness profiles are unavailable*, leaves the same deposit — materialisation happens before the profile is validated and is not undone by the refusal. Then `uninstall --yes` sets the reference index to **zero references and deletes no object**. So a store on this machine grows by every review and every refused install, and `LAF-105` is why it cannot shrink: `collect_garbage` implements the sweep and no command reaches it. This is a measured mechanism for the ratio `LAF-105` recorded on the real data root — 79 objects against 34 references. **Measured further the next iteration, and two things in this row were wrong.** (1) The growth is **not** per invocation: the store is content-addressed, so the same review run three times leaves the same two objects — it grows once per distinct object ever looked at, and never shrinks. (2) The review is not the earliest depositing path: **`aart marketplace status --profile claude` deposits the same two objects**, and it is a query nobody would expect to write. `marketplace list` deposits nothing, `source sync` deposits nothing. So the rule is: the first command that resolves an artifact to its content materialises it, whatever that command is called, and no command ever removes it. **Severity corrected `high` → `medium` on that measurement**: the claim in the help text is still false and the deposit is still unremovable, but the growth is bounded by the number of distinct artifact versions the operator has ever looked at, not by how often they look. `LAF-117` is what that costs when a source goes away |
| `LAF-115` | medium | walking the machine channel cold | `open` | — the one command group written for a machine has no machine channel. Measured `2026-08-16` on the wheel built at this commit (`LA-0-14`, `LAS-66`): **34 of the 38 leaves accept `--json`; the four that do not are `upgrade` and all three of `reporting` — `validate-event`, `validate-issue`, `aggregate`.** `reporting` exists for registry-owned usage reports, whose consumer is CI, and `aart reporting validate-event <file>` on an invalid file prints exactly one line, `usage report is invalid`, and exits `1`. No code, no field, no pointer, no remediation, and no way to ask for structure. The same walk shows what the rest of the surface gives a consumer in the same situation: `marketplace list --json` returns `ok:false`, `operation`, and a diagnostic with code `no-source-configured` and a remediation sentence. `SPEC-aart-1.0.md` §21 says core operations return typed results and the TUI does not parse human stdout; for this group there is nothing else to parse |
| `LAF-114` | low | walking the machine channel cold | `open` | — three of the sixteen *initial stable error codes* in `SPEC-aart-1.0.md` §21 are emitted nowhere. `import-lossy`, `import-stale` and `lock-stale` appear in the package **only** inside the `INITIAL_ERROR_CODES` tuple in `domain/diagnostics.py:11`; a fourth, `no-source-configured`, has exactly one emission site (`application/configuration.py:237`). The declared vocabulary is also unenforced: `INITIAL_ERROR_CODES` has one reference in the whole repository, in `tests/domain_kernel_test.py`, so nothing at runtime requires an emitted code to be in it, and 124 distinct codes are constructed through `DiagnosticCode("…")` literals. Where the SPEC's vocabulary offers `lock-stale`, the store emits `store-unavailable` and `store-invalid` (`io/object_store.py:32`). The SPEC says *include*, so this is not a contradiction — it is a consumer writing a handler for `lock-stale` or `import-stale` and waiting forever. Severity `low`, and it turns on a decision: either those three conditions are unreachable, in which case the list is stale, or they report under other codes, in which case the list is wrong |
| `LAF-113` | medium | walking every command cold | `open` | — `SPEC-aart-1.0.md` §20 disagrees with the shipped command surface in both directions at once, and it is a *checked current* design document, not a dated one. It says the implemented slice is `source add|list` and `marketplace list [--json]`. The wheel built at this commit ships **38 leaves under 6 groups**: `registry` 13, `marketplace` 8 including `receipt show|verify|undo`, `source` 6, `security` 5, `reporting` 3, `upgrade` 1. So the section understates what is implemented by more than an order of magnitude. It then describes the design targets as *not aliases for the retained legacy `list/install/update/setup` compatibility commands* — and **none of those four exists**: `aart list`, `aart install`, `aart update` and `aart setup` each print the top-level usage and refuse. A reader of the specification is told that less ships than does, and that four commands are retained which are gone. The same walk confirms two counts other rows rest on: `LAF-105`'s *38 subcommands* is exact, and the v1 record in `LA-0-02` of *15 top-level / 49 leaves* is now `6 / 38` — consolidation into groups, not removal of function. Not repaired; a design edit is a decision about what `1.0` means, not a typo |
| `LAF-112` | low | walking every command cold | `open` | — the `registry` group renders failure two ways. Measured `2026-08-16` on the wheel built at this commit, every leaf run with no arguments in an empty directory under a sandbox `HOME` (`LA-0-13`, `LAS-65`). `registry validate`, `audit` and `test` print a header — `registry validate: failed` — and then an indented `error: registry workspace requires aart-registry.json`. `registry format`, `lock`, `build` and `diff` print a bare `error: …` with no header and no command name in it. Same group, same class of refusal — the workspace or the writable checkout is not there — two shapes, so anything grepping a failure for the command that produced it finds three of seven. None of the seven carries a `remediation:` line on `main`, which is `RS-09`, closed on `fix/registry-refusals-carry-remediation-rs09` and not merged; the `marketplace` group in the same walk prints `remediation:` for all six of its refusals, which is what the fixed shape looks like. Severity `low`: nothing misbehaves, the words are just not the same words. **Only the human channel is ambiguous**: walked again with `--json` the next iteration, the two shapes carry different typed codes — `registry-command-invalid` for the workspace refusals and `registry-workspace-invalid` for the checkout ones — so a machine consumer can tell them apart. `RS-09` is visible there too, structurally: those diagnostics carry `remediation: []` where `marketplace`'s carry a sentence |
| `LAF-111` | low | reading `LAF-106`'s eight against the shipped CLI | `open` | — two of `LAF-106`'s eight are bypassed wrappers, not missing doors, and the file that bypasses them reaches the application layer for the command next door. `aart registry format` **works**: walked `2026-08-16` on the wheel built at this commit, in a throwaway Git checkout after `registry init --source-id lab --display-name … --yes`, it prints its plan and *unchanged* for both files and exits `0` (`LA-R-43`). It gets there through `plan_registry_format` called **directly** from `curation/runtime.py:856`, not through `application/registry_commands.py:prepare_registry_format`, which is why the wrapper measures unreachable. `plan_native_promotion` is called the same way at `:894`, behind `aart registry promote-native`. And the same file calls the application wrapper `prepare_registry_init` at `:417`. One runtime, two layers, three neighbouring commands. This does not change what `LAF-106` measured, it changes what to do about it: of the eight, **three are doors the design names and the CLI has not got** — `collect_garbage` and `object_status` (`SPEC-aart-1.0.md` §20 lists `aart store status|verify|gc` among design targets, and §16 specifies `store gc` as dry-run by default under a global lock, which `application/store.py:110` implements exactly, `GcRequest.execute` defaulting to `False`) and `compile_sources` (`aart compile`, same list) — **two are bypassed wrappers**, and **three are superseded configuration writers**, of which `recover_user_configuration` has neither twin nor command |
| `LAF-110` | medium | reading `LAF-108`'s security cluster against the shipped CLI | `open` | — AART lists analyzers it cannot run. On the wheel built at this commit (`fcdf95d9…`), installed into a throwaway venv, `aart security analyzers` prints `ruff: available` and four *not installed*, and `aart security suites` describes three suites of which two promise to *add every discovered analyzer* and *add locally installed static, secret and shell analyzers*. **No command runs one.** `security scan --help` offers `--index`, `--artifact`, `--lock`, `--cache`, `--json` and nothing that selects a suite or a provider; the string `suite` appears in `cli.py` exactly once, in the help text of the listing command. Every function that would execute a provider is unreachable: `io/security_analyzers.py:run_analyzer_process` (`LAF-107`), `security/tool_adapters.py:run_tool_adapter`, `security/analyzers.py:run_protocol_analyzer` with the handshake and scan encoders, and `security/policy.py:evaluate_security_policy` with `summarize_bundle_security` under it (`LAF-108`). The discovery half *is* wired — `discover_tool_adapters` runs from `commands/security.py:117`, which is how the availability line is produced — so the shipped tool can tell an operator which analyzers are installed and can use none of them. Distinct from `LAF-15`, which is that no command emits the envelope `scan` consumes: that one is about input, this one is that no command runs a provider even when the input is in hand. Severity `medium`: nothing here is unsafe, but two commands make a capability claim the executable cannot honour, and an operator installs `bandit` on the strength of it (`LA-R-42`, `LAS-63`) |
| `LAF-109` | low | reading `LAF-108`'s clusters | `open` | — the same thing is implemented twice, and the copy the tests exercise is the dead one. `agent_artifacts/fp.py` and `agent_artifacts/domain/result.py` both define `bind`, `collect`, `is_ok`, `is_err`, `map_ok` and `map_err`. Dozens of modules import `Ok`, `Err` and `Result` **from `domain/result.py`** and call none of its combinators; **nothing in the package imports `fp.py` at all** — its only references outside itself are `tests/smoke_test.py` and its own docstring, which tells a reader to write `from agent_artifacts.fp import Ok, Err, bind`. `agent_artifacts/hashing.py` (22 lines) and `agent_artifacts/protocol/hashing.py` both define `sha256_bytes`; the package imports the `protocol` one everywhere and the root one nowhere, and the root one has no test either. The measurement in `LAF-108` shows the cost directly: `hashing.py:sha256_bytes` counts as *reached* there only because a live twin shares its name. So a reader looking for this project's `Result` helpers or its hashing helper finds two answers, and the one `docs/plan/PLAN.md` names — `fp` — is the one nothing runs |
| `LAF-108` | medium | the same measurement across the whole package | `open` | — the unreachable surface is the package's habit, not `application/`'s. Every module under `agent_artifacts/` measured on `2026-08-16` by one rule, stated so it can be argued with: a public top-level function is *reached* when its bare name is reachable from a seed, seeds are every name a module refers to at module scope (dispatch tables, class bodies, decorators, defaults) plus `main` for the console script `agent_artifacts.cli:main`, and a reachable function makes every name in its body reachable. **103 of 482 public top-level functions are unreachable — a fifth of the surface.** The rule is name-based, so it over-approximates: any same-named function anywhere in the package rescues a dead one, which makes 103 a floor, not an estimate. Clusters, largest first. **`security/`: 15 of 39.** `evaluate_security_policy` has no caller, and `summarize_bundle_security` is called only from inside it (`security/policy.py:111`), so the whole policy-evaluation chain is dead; `run_protocol_analyzer`, `to_security_assessment`, `parse_scan_result`, `parse_handshake` and both request encoders go with it. `commands/security.py` is the only security command and it imports attestations, baseline, cache and projections — never these. That is `LAF-15`'s mechanism generalised: the analyzer half of the security package is unwired end to end, adapter (`LAF-107`) and policy alike. **`domain/` 13 of 19 with `fp.py` 10 of 11** — two functional cores, both dead: `LAF-109`. **`compiler/` 9 of 12**, the far side of `LAF-106`'s `compile_sources`. **`application/` 8 of 27 — the identical eight names `LAF-106` lists**, re-derived by a differently written script, which is the closest thing this run has to a replication. **Five root modules have no importer anywhere in the package**: `compatibility.py` (111 lines), `fp.py` (96), `hashing.py` (22), `policy.py` (67), `registry_publication.py` (506). All five ship — read out of the wheel built at this commit, `agent_artifacts-2.6.0-py3-none-any.whl`, 542 151 bytes, sha256 `fcdf95d9…`. Four have a test file and no caller; `hashing.py` has neither. `registry_publication.py` is not a new observation: `LAF-03` recorded it on `2026-08-13` and it is still true. Nothing repaired. Deleting on this measurement alone would be wrong — `LAF-106` found two of its eight were honest supersession, and each of these needs the same reading |
| `LAF-107` | low | the same measurement one layer out | `open` | — the unreachable surface is an `application/` and `io/` habit, and `store/` is clean. Measured `2026-08-16` by reachability rather than by grep: a function counts as reached if a reachable function calls it or any module refers to it outside an import. `agent_artifacts/store/`: **0 of 5 unreachable**. `agent_artifacts/io/`: **11**, and they cluster. Four are `io/security_analyzers.py` — `run_analyzer_process`, `write_input`, `read_stream`, `stop_process`; only `resolve_executable` from that module is imported by `commands/security.py`, so the process runner beside it is unwired, which is the mechanism half of `LAF-15`'s complaint that `security scan` has no command that feeds it. Four are `io/fs.py` utilities — `copy_tree`, `read_json`, `remove_path`, `symlink_tree` — each with tests and no caller; `symlink_tree` is **not** a missing feature, `--install-mode symlink` is implemented in `installation/application.py` with its own handling, so it is a superseded helper. Two are `io/cache.py`, which is `RS-05`'s dead module, counted here for completeness. One is `materialize_compiler_object`, which belongs to the compiler orchestration `LAF-106` records as unreachable — the same hole seen from the adapter side. Severity `low` against `LAF-106`'s `medium`: these are helpers and adapters, not entry points a design promised. **Corrected `2026-08-16`, one iteration later: `io/` is 8, not 11.** `write_input`, `read_stream` and `stop_process` are nested helpers *inside* `run_analyzer_process` (`io/security_analyzers.py:84`, `:90`, `:113`), not functions of their own, and the script that counted them did not tell the difference. The module still has exactly one unwired public entry point, so the reading stands and only the arithmetic changes; `io/fs.py`, `io/cache.py` and `io/object_store.py` are unaffected. `LAF-108` re-derives the corrected count |
| `LAF-106` | medium | generalising `LAF-105` | `open` | — `LAF-105` is not one door, it is eight. Every public function in `agent_artifacts/application/` was measured on `2026-08-16` for callers outside its own module, `application/__init__.py` and `tests/`. Re-measured the next iteration by call-graph reachability, which counts a same-module caller and a module-level reference — a rule the first pass would have been wrong under, and all eight survive it. Twenty have one or more; **eight have none**: `collect_garbage` and `object_status` (`store.py`), `compile_sources` (`compiler.py` — and with it `CompilerPorts`, `CompilerSteps` and every `CompilerRequest`, built nowhere but the compiler package itself), `prepare_registry_format` (`registry_commands.py`), `prepare_native_promotion` (`registry_maintenance.py`), and `save_user_configuration`, `save_user_configuration_for_source_management` and `recover_user_configuration` (`configuration.py`). Two of the eight are superseded and say so: `save_user_configuration_checked`'s docstring names itself *the CFG02 replacement for* one of them, and the other is the unchecked write beside it. The remaining six are simply unwired. All eight are exercised by tests — `compile_sources` alone is called from fifteen places in `tests/compiler_pipeline_test.py` — so coverage counts them as reached while no operator can reach them. That is the cost: the suite's green says these paths work, and says nothing about whether anything runs them |
| `LAF-105` | medium | tracing `LAF-85`'s mechanism | `open` | — the object store's garbage collector cannot be run. `application/store.py:110` implements `collect_garbage` — acquire the store lease, read the references, inventory the objects, delete every digest no reference names — and **nothing in the package calls it**. It is exported from `agent_artifacts.application` and `GcRequest` from `agent_artifacts.store`, and the only callers anywhere are `tests/store_e2e_test.py` and `tests/store_gc_application_test.py`, both on temporary roots. None of the CLI's 38 subcommands reaches it. So the durable store only grows: measured read-only on this machine's real data root on `2026-08-16`, 79 object entries against 34 reference entries in `state/object-references.json`. Unlike `RS-05`'s dead module this one is finished, tested and correct — it is the door that is missing, not the room. **The ratio now has a measured cause**: `LAF-116` shows a plan-only review and a refused install both deposit objects that no reference ever names, so the gap between 79 objects and 34 references is what those two paths leave behind, with nothing able to sweep it **Costed `2026-08-16`: the collector is not missing, only its verb.** Every part of the chain ships — `collect_garbage` (`application/store.py:110`), `GcRequest` with `execute: bool = False`, `GcPlan`, and both filesystem adapters the ports need, `inventory_objects` (`io/object_store.py:469`) and `delete_object` (`:510`), all present on `main`. `docs/store/content-addressed-store-v1.md` describes plan-or-execute through injected ports, and `SPEC-aart-1.0.md` lists *global garbage collection* among the operations the lock protocol covers. Nothing in `cli.py` reaches any of it. So the change that closes this — and gives `LAF-116` and `LAF-117` the removal path they currently lack — is a command surface over code that already exists and is already specified |
| `LAF-104` | low | auditing this register against the stream | `open` | — the *Scope* paragraph describes a table two sizes and one origin out of date. It says this file was seeded from *the twenty-eight items of the `2026-08-15` stream*; the stream's tables carry **30** ids. Its own opening says *Twenty-eight deferred items* and was written in [`2e32975`](https://github.com/M1F1/agent-artifacts/commit/2e32975), before `LAF-63` was appended in `70a737a` and `LAF-64` in `3b04d5f`; the register was created one commit later, in `3ff834e`, and copied the count that was already stale. Neither number misplaces a row — the scope boundary is stream *membership*, not the count — and the direction `DOC008` enforces holds exactly: all 30 stream ids have a row here. The second half is the origins. *Seeded from the stream, plus what implementing the response to it found* names two; of the 24 rows with no stream row, 11 are the `2.6.0` work and **13 come from auditing the response rather than implementing it** — the `re-checking`, `re-reading` and `auditing` sources filed during the `2026-08-15` overnight run, which have no stream and are not covered by either phrase |
| `LAF-103` | medium | auditing the live-acceptance run headers | `open` | — the newest run document names neither the commit it ran nor a digest, and it is the one making the strongest claim about its executable. `DESIGN-live-acceptance-v1.md` §3 requires the executable *pinned to one commit for the whole run*; the four fields the later runs converged on are tag or commit, wheel name and size, wheel sha256, `aart --version`. Read by hand on `main`: `PROGRESS-live-acceptance.md` has commit, wheel and size, version, and **no digest row at all**; `-v2.md` has all four but its digest is written `a2edb0dc…4f47e`, 13 of 64 characters, which identifies no file; `-v3.md` and `-setup-build.md` carry all four with a full 64-character digest. `-receipt.md` carries **none of them** — its heading is `## Run root`, its executable row is a bare filename with no size, and the words `commit`, `sha256` and `aart --version` appear nowhere in its header, while its second paragraph says *No patched executable … Everything below runs the wheel built from the committed tree*. Which tree is not stated. `2.6.0` on `main` does build reproducibly — `fcdf95d9…`, 542 151 bytes — but the document does not say that is what ran, so the one claim it makes about its own executable is the one thing it leaves unverifiable. Same cause as `LAF-102`: that walk was written as a design's acceptance check, not as a live-acceptance run, and it cites `DESIGN-readable-receipt.md` rather than the design that carries the pinning rule |
| `LAF-102` | medium | auditing the live-acceptance documents | `open` | — the scenario map says *every scenario has a stable ID* and *a future run re-executes these IDs*, and most later runs do not use it. On `main`, `PROGRESS-live-acceptance.md` walks 75 ids and every one is declared; `PROGRESS-live-acceptance-v2.md` and `-v3.md` contain **no scenario id at all**; `-receipt.md` walks a design's numbered acceptance criteria instead; and `-setup-build.md` walks 25 ids in a `LAB-*` namespace the map never declares — it declares only `LA-0`, `LA-M`, `LA-R`, `LA-S`, `LA-U`. The cause is legible in the title: the file is *Live acceptance **v1** — scenario map*, written for one run, and each later run invented its own record rather than appending. This is `LAF-87` one level up: that finding is about stressor ids spanning four documents, this is the scenarios themselves. The append-only rule itself holds — the file's whole history is two commits and the second only appends `LAS-29` and `LAS-30`, deleting nothing |
| `LAF-101` | high | auditing the register's shape | `open` | — the register was seeded from one walk's findings table and took the open ones. `PROGRESS-live-acceptance-setup-build.md` records `LAF-51`..`LAF-61` in a single table; this register carries `LAF-52`..`LAF-55`, `LAF-57`..`LAF-59` and `LAF-61`, and has no row for `LAF-51`, `LAF-56` or `LAF-60`. Those three are exactly the ones that were resolved — the document says *`LAF-51` is closed* and *`LAF-56` and `LAF-60` are documentation fixes that landed with `SBC-9`*, two of them `major`. The *Scope* rule does not separate them from their eight neighbours: all eleven come from the same pre-stream walk. So three closures live in a sentence in a run log, which is the form of record `C6` and this register were written to replace |
| `LAF-100` | medium | auditing the register's shape | `open` | — this file's opening says `docs_check` *fails when a document names a finding this file does not carry*. It does not. `DOC008` reads rows of a table in files matching `docs/testing/residue-stream-*.md` and nothing else; a checked design or plan may name any id with no row and the gate stays green. Measured on `2026-08-16`: 28 finding ids are named in the 60 checked documents with no register row, and `make docs-check` passes. Most are pre-stream ids the *Scope* section places outside, which is the defensible half — the claim in the opening paragraph is the part that is not true as written |
| `LAF-99` | medium | re-reading the `deferred` disposition | `open` | — `deferred` is defined as *out of scope by an explicit decision recorded in a design, not by neglect*, and `LAF-62` is the only row carrying it. Nothing records that decision. The commit that measured the finding, [`6589778`](https://github.com/M1F1/agent-artifacts/commit/6589778), ends *Recorded as `LAF-62` and left open* and says the design question is unanswered; the register was already carrying `deferred` from [`3ff834e`](https://github.com/M1F1/agent-artifacts/commit/3ff834e), the commit that created the table. The only design mention is `DESIGN-readable-receipt.md` §5 — *it does not touch the index-version boundary … those need their own stream* — which records that one design's scope, not a decision about `LAF-62`. Two findings in one: a disposition weaker than its own definition, and no gate that could notice, because `DOC006`..`DOC009` check documents against the register and nothing checks the register against itself |
| `LAF-98` | low | re-reading `LAF-68`'s claim | `open` | — the acceptance runner installs the release asset and never checks it. `.github/workflows/consumer-acceptance.yml:32` in the acceptance repository runs `python -m pip install --no-deps "https://github.com/M1F1/agent-artifacts/releases/download/v${AART_VERSION}/agent_artifacts-…​.whl"`, and the file contains no `sha256`, `shasum` or digest step at all. `LAF-70`'s row records that the authoring machine installed the same asset *after verifying its digest against `wheel-digest`*; the unattended runner that reconciles eleven installations every morning does not. `release.py wheel-digest` exists to make that check possible and nothing in the constellation's CI calls it |
| `LAF-97` | medium | re-reading `LAF-74`'s claim | `open` | — the gate that catches a stale *shipped open* claim reads only what the checked list names, and `CHANGELOG.md` is not on it. The list is `docs/plan/*.md`, `docs/design/*.md`, `compatibility-v14.md`, `release-checklist-v14.md` — 60 files. Running `DOC009`'s own logic over every markdown file outside it: `CHANGELOG.md:60` would fail for `LAF-63`, which its `## 2.6.0` section lists under *Known defects shipped open* while the register has it `closed` by `RR-10A` — work that shipped **in** `2.6.0`. That is the identical stale claim `LAF-74` found in `github-release-v2.6.0.md`, in the document beside it, uncaught. The other two ungated hits — `release-checklist-v13.md` and the stream's *From `2.5.0`* section — are dated records of `2.5.0` and correctly outside. `LAF-74` stays `closed`: its evidence reproduces, and adding one file to the list is what it claims |
| `LAF-96` | medium | re-reading `LAF-66`'s claim, live | `open` | — `receipt verify`'s two record-wide claims render as their own opposite. The renderer composes `f"{status}: {subject}"` (`setup_render.py:85`) and both subjects name the *bad* condition while both kinds are negative. On a real run against a locally built `2.6.0` wheel (sha256 `fcdf95d9…`) the output read `true: credential-shaped text in the persisted record` with `detail  no credential-shaped text in the record`, and `false: working copies left by plan b233f1dff6080db4` with `detail  1 working copy left by an interrupted run, not removed: …`. Both headlines say the reverse of their own detail. Confined to `no-credential-in-record` and `no-orphan-run-directory`: every step claim's subject is a bare noun — an image, a tag, a path — where `status: subject` reads correctly |
| `LAF-95` | medium | re-reading `LAF-59`'s claim | `open` | — a long failing instruction is reported as a fragment, and the line that says a build failed is what goes. Measured on two real failing builds. A 708-character `RUN` produces 5 907 characters of BuildKit output; `failure_detail` keeps 512, and the tail begins mid-word at `gument-list-that-real-recipes-do-carry; …`. `exit code: 4` and `did not complete successfully` survive because they are last; `ERROR: failed to build: failed to solve: process` does not appear at all, nor does the `>>>` context marker naming the Dockerfile line. The head is the other half: 128 of the 512 characters go to `#0 building with "desktop-linux" instance using docker driver … #1 transferri`, cut mid-word, in both runs — progress boilerplate, which is the half `LAF-59` was raised about. `DESIGN-readable-receipt.md` §3.5 says both ends carry meaning; for `docker build` the head measurably does not |
| `LAF-94` | low | re-reading `LAF-72`'s claim | `open` | — the token-containment test says channels 2 **and 4** are walked structurally, "so a field added later and forgotten fails this test". True of channel 2, which walks `dump_setup_state` output — the real writer. Not true of channel 4: `test_the_setup_payload_and_its_text_carry_no_planted_value` builds its own `payload` literal with `planned` and `planning_failures` empty, so the walk visits 11 strings the test wrote, and never calls `_setup_payload`. One real `planned` row carries 25, all of them recipe-derived, and a real run has one per queued artifact. Measured, not asserted: the containment is real today — `project_setup_review` redacts a purpose-borne credential before `_setup_plan_payload` reads it (`purpose` came back `Configure access using [redacted]`) — so this is a coverage claim wider than its evidence, not an exposure. `DESIGN-token-containment.md` §4.3 makes the same claim about channels generally and is left standing |
| `LAF-93` | medium | re-checking this run's own closures | `open` | — a `closed` row can name evidence that no command will run, and nothing notices. `RS-07`'s row on `fix/status-names-the-missing-source-rs07` names `tests/identity_change_reconciliation_test.py::test_rs07_status_reports_the_project_when_the_only_subscription_is_removed`; the test is a method of `IdentityChangeReconciliationTest`, so pytest answers `no match in any of [<Module identity_change_reconciliation_test.py>]`, exits `4`, and prints `no tests ran`. The fix itself is sound — the same test passes when the class is named — so what is wrong is the record, and its failure mode is silence rather than a red test. Three further rows (`RS-08`, `LAF-45`, `LAF-73`, each on its own branch) name a `test_laf45_*` glob, which pytest also refuses as a node id and needs `-k` instead. `scripts/docs_check.py` checks that links resolve and that stream rows reach the register; it never tries the evidence. Found by re-running every row this run closed |
| `LAF-92` | medium | re-checking `LAF-71` | `open` | — Registry B's CI still runs AART `v2.5.0` against a `2.6.0` constellation, and a pull request title is what said otherwise. [#5](https://github.com/M1F1/agent-artifacts-registry-2/pull/5) is titled *Move the CI pin to 2.6.0*, merged from a branch named `chore/move-pin-2.5.0`, and its diff moves all three workflows from `v2.0.0` to `v2.5.0`. Nothing has moved them since and no `AART_REF` repository variable overrides the default. Two findings in one: the stale pin, and that a closure was recorded from a title rather than a diff |
| `LAF-91` | medium | re-checking `LAF-53` | `open` | — a completed undo reports itself as `skipped`. `marketplace receipt undo … --yes` removes what the run created, prints `reverses=1, keeps=0`, and then ends `Undo outcome: skipped — Setup rollback completed`; `--json` carries `"ok": true` beside `"status": "skipped"`. The value is the artifact's *post-undo setup state*, written at `setup_runtime.py:1369`, and it is correct as a state. Rendered as the outcome of the operation the operator just asked for, it says the opposite of what happened. **`LAF-84` is this same defect**, filed eleven iterations earlier from the `LAF-73` walk and never given a row here; the two ids are one finding, and this row is the one carrying the evidence |
| `LAF-90` | high | implementing `RS-02`, branch `fix/registry-requests-stop-stamping-dead-bounds-rs02` | `open` | — the curses wizard's own suggested defaults author a registry the same AART then refuses to read. the `RS-02` fix on that branch removed two dead compatibility literals from every registry request; the wizard still offers the same two as its defaults for `registry init`, and there they are not dead. An operator who presses return at both prompts gets a registry declaring `>=1.0.0,<2.0.0`, and `registry validate` on it answers *registry workspace is incompatible with this AART version*. Both halves were reproduced without a terminal — the wizard's question loop takes its reader as an argument — so only the screen is human-gated, not the defect. **Walked end to end on `2026-08-16`** against a locally built `2.6.0` wheel (`fcdf95d9…`, 542 151 bytes, commit `e3894fe`) in a throwaway venv under a sandbox `HOME`, as `LA-0-11` and `LA-0-12`, stressor `LAS-62`. The literals are `tui.py:2793` and `:2796` — *Minimum AART version [1.0.0]* and *Maximum AART version (exclusive) [2.0.0]* — and `:2806` falls back to the same two when the operator presses return. `registry init` with them exits `0`, writes `requires_aart {min_inclusive: 1.0.0, max_exclusive: 2.0.0}`, and ends by advising four next commands. The first of them, `registry validate`, exits `1` with `registry-maintenance-invalid`, *registry workspace is incompatible with this AART version*, and `"remediation": []`. The control (`LA-0-12`) is what makes it a defect rather than a policy: the **flag** defaults are `2.6.0` and `3.0.0` — `init --help` prints them — and a registry authored with those validates, exit `0`. Two default sets in one product, and the one an operator reaches by pressing return is the stale one **Costed `2026-08-16`, and the fix is four literals with a home to go to.** `tui.py:2793` and `:2796` hold the prompt defaults, `:2806` and `:2807` the fallbacks when an operator presses return — four occurrences of `1.0.0`/`2.0.0`. The values they should carry already exist two files away: `curation/model.py:26` computes `_DEFAULT_MINIMUM_AART = str(EXECUTABLE_VERSION)` and `_DEFAULT_MAXIMUM_AART` as the next major, both on `main`, above a comment that states this defect exactly — *literals here go stale on every release and, once the floor reaches the old literal ceiling, make the pair unsatisfiable*. So the code that explains the bug and the code that still commits it are in the same package; the fix is to export those two constants and use them, not to invent a rule |
| `LAF-89` | low | implementing `LAF-47`/`RS-10`, branch `fix/uninstall-removes-the-file-it-made` | `open` | — whether the merge file is reclaimed depends on uninstall order. `created_destination` is recorded per effect, and the record carrying `true` is deleted by the first uninstall, so uninstalling in install order leaves the file and uninstalling in reverse order removes it. `LA-U-35` walks the asymmetry both ways. The design named this case before the walk found it, which is why it is the remainder of the `LAF-47` fix rather than a reopening of it. **Re-walked `2026-08-16`** on a wheel built from that branch (`73426b33…`, 542 648 bytes, commit `76a7f21`), re-executing `LA-U-35` rather than inventing an id: two `mcp` artifacts installed in separate commands, uninstalled in install order leave `.mcp.json` as `{"mcpServers":{}}` with `?? .mcp.json`; the same walk in a fresh repository uninstalled in reverse order removes the file and leaves the tree clean. Severity corrected from `medium` to `low` — `PROGRESS-live-acceptance-v11.md`'s findings table records `low`, and this row was transcribed from a run-log sentence rather than from it. **The asymmetry is the branch's, not `main`'s** (`2026-08-16`, `LA-U-40`): on a wheel built from `main`, two `mcp` artifacts uninstalled in install order and in reverse order leave the *same* result — `.mcp.json` holding `{"mcpServers":{}}` in both. So the fix removes the file in one order and leaves `main`'s behaviour in the other; it takes nothing away, and this row is the remainder of the improvement rather than a regression in it |
| `LAF-88` | low | implementing `LAF-47`/`RS-10`, branch `fix/uninstall-removes-the-file-it-made` | `open` | — the emptied directory outlives the file. `uninstall` now removes `.claude/settings.json`, and `.claude/` stays behind, empty. `git status` cannot see it because git does not track empty directories, so the workspace looks clean and is not. **Re-walked `2026-08-16`** on the same branch wheel, re-executing `LA-U-31`: install one `hook`, uninstall it, and `git status --porcelain` prints nothing while `ls -la .claude` shows a directory holding only `.` and `..`. One detail the row does not have: the *deeper* directory is reclaimed — `.claude/hooks/` and `settings.json` are both gone — so what survives is exactly the one directory level the artifact did not itself fill. **Wider than hooks, and older than the branch it was found on** (`2026-08-16`, `LA-U-39` walk): on a wheel built from `main`, installing a *skill* and uninstalling it leaves `~/.claude/` at user scope and `.claude/` at project scope, both empty, with `.claude/skills/code-review/` and the payload gone. So this is not something the fix branch introduced and it is not specific to `settings.json` — every artifact whose destination lives under the harness directory leaves that one directory behind |
| `LAF-87` | medium | walking `LAF-45` live, branch `fix/audit-upstream-says-it-checked-laf45` | `open` | — the live-acceptance stressor namespace spans four documents and no single place knows the highest id in use. `LAS-*` ids run to `LAS-56` across the v1, v2, v3 and setup-build records while `live-acceptance-scenarios.md`'s own table stops at `LAS-30`, as if `LAS-31` were free. It is not, and three branches of the `2026-08-15` run each re-used an id that already meant something else before the collision was noticed. `LAF-102` is this one level up, for the scenario ids rather than the stressors |
| `LAF-86` | low | implementing `RS-07`, branch `fix/status-names-the-missing-source-rs07` | `open` | — `uninstall` with no coordinate advises `marketplace list`, and `list` refuses when no source is configured. The advice is for the one state in which it cannot be followed. `status` was fixed to read the project with no source; `install`, `update`, `list` and `setup` still refuse, which is correct for four of them and leaves this remediation line pointing at the fifth. Re-checked on `main` `2026-08-16`: the line is shared, not per-command — `_selection` (`commands/marketplace.py:332`) emits *run `aart marketplace list --json` to see available coordinates* for every action in `_REQUIRES_COORDINATES`, which is `install`, `uninstall`, `setup` (`:96`). Right for two of the three |
| `LAF-85` | high | walking `LAF-73` live, branch `fix/receipt-verify-stale-rollback-laf73` | `open` | — something wrote to the operator's real data root at `23:34`–`23:36` on `2026-08-15`, during an unattended session in which every live scenario ran under a sandbox `HOME`. `make integration`, `unit`, `validate` and `packaging-check` were each measured afterwards and touched nothing there, so the writer is unidentified. Recorded rather than shrugged at a second time. Nothing is known to be damaged; what is not known is what wrote. **Re-read `2026-08-16`, read-only — `ls`, `find`, `stat` and one `json.load`; nothing under the real root was written, moved or removed.** Four things are now known that the row did not say. (1) **43 of the 68 object shard directories carry mtime `2026-08-15 23:34`**, and **no object entry in the store has a birth time later than `20:12`** — a directory mtime moves on creation or removal, and nothing was created, so the episode *removed* content rather than depositing it. (2) Exactly one file changed: `state/object-references.json`, born `23:36`, now holding 34 references — AART writes atomically, so a rewrite gives a new birth time and this does not distinguish a first write from a later one. (3) `locks/` carries `23:36` and is empty, so a lock was taken and released; `state/setup/` is untouched since `2026-08-11 19:35`, so no setup record was written. (4) **Nothing in the entire root has changed since**: zero entries newer than `2026-08-16 00:00`, across a night of continuous work under sandbox `HOME`s. So it was one episode, it left nothing behind, and it has not recurred. **The mechanism is ambiguous and the first reading of it was too strong** (`2026-08-16`, second pass): an install followed by an uninstall produces the same trace as a sweep — entries created and then removed move a shard's mtime and leave no surviving birth, and `write_references` is wired into `lifecycle/io.py`, `installation/io.py` and `setup_engine/io.py`, so an ordinary uninstall rewrites the index under exactly this lock. The one code path that *deletes* objects, `collect_garbage`, has no caller at all (`LAF-105`), which makes the install-then-uninstall pair the likelier of the two. The writer is still unidentified. **The mechanism is now reproduced live** (`2026-08-16`, third pass, `LA-U-37`): in a sandbox with a `source-local` source, an `install --yes` of objects that already existed moved both shard directory mtimes **without giving any object a new birth time**, and the following `uninstall --yes` moved them again, rewrote `state/object-references.json` — new birth time, zero references — and left `locks/` empty with a fresh mtime. That is this row's four observations, item for item, produced by an ordinary install-then-uninstall. **So the first reading is refuted by measurement, not by argument**: a shard mtime moving without a birth does not mean anything was removed, because an install of already-present content moves it too, and an uninstall deletes no object at all. What wrote at `23:34` is still not named, but the shape no longer points at a sweep |
| `LAF-84` | medium | walking `LAF-73` live, branch `fix/receipt-verify-stale-rollback-laf73` | `open` | — a completed undo leaves the receipt reading `skipped`, a word that in this vocabulary means the setup never ran. **The same finding was filed again eleven iterations later as `LAF-91`**, because this id was named only in a run log and a commit message. Both rows are left standing: `LAF-91` carries the fuller evidence, and which id retires is the maintainer's call. This pair is the measured cost of the fifteen missing rows |
| `LAF-83` | low | implementing `LAF-69` (`DOC010`), branch `fix/docs-check-both-directions-laf69` | `open` | — `DOC010` reads a disposition only in the form the documents already use: one table cell holding one disposition beside the finding. A document that makes the same claim in prose is not checked, and deliberately so — deciding a disposition by parsing sentences is what the register exists to stop documents doing. Recorded as the uncovered half rather than counted as fixed |
| `LAF-82` | medium | implementing `LAF-69` (`DOC010`), branch `fix/docs-check-both-directions-laf69` | `open` | — this register's opening claims a document naming a finding it does not carry will fail the gate, and no rule implements that. **Filed again as `LAF-100`**, with the measurement — 28 such ids across the 60 checked documents, gate green — because this id existed only in a commit message. Two ids, one claim; `LAF-100` is the one with the numbers |
| `LAF-81` | medium | walking `LAF-75` live, branch `fix/wheel-digest-emits-what-it-hashes-laf75` | `open` | — `wheel-digest` builds the working tree while stamping `HEAD`. The digest it prints therefore describes the commit only when the tree is clean, and nothing in the command says which of the two it hashed. Measured on `2026-08-16` in a detached worktree at `e3894fe`: clean, `wheel-digest` prints `sha256:8ed1226d…`; with one uncommitted comment line appended to `agent_artifacts/redaction.py` and `HEAD` unmoved, it prints `sha256:19ca00a9…` — same filename, exit `0`, no warning. `_copy_project` copies the working tree from disk, and `_commit.py` is then written from `git rev-parse HEAD`, so the wheel carries the tag's stamp over the desk's bytes. The command's own help says *print the digest of the wheel this commit publishes*, which is the claim it breaks. `LAF-75`'s fix made the printed digest the digest of the file left in `dist/`; this is the remaining gap between that file and the commit it claims |
| `LAF-80` | low | walking `LAF-75` live, branch `fix/wheel-digest-emits-what-it-hashes-laf75` | `open` | — `make wheel` dirties a tracked file and no document says so. An operator following the release checklist sees an unexpected `git status` at the step where a clean tree is what they are checking for. Measured in a detached worktree at `e3894fe` on `2026-08-16`: the tree is clean, `make wheel` runs `inject_commit.py`, and `git status --porcelain` then reports ` M agent_artifacts/_commit.py` — the committed value is `COMMIT = "unknown"` and the build replaces it with the real sha and epoch. `wheel-reproducibility-v1.md` §*Verifying a published wheel* tells a verifier to `git checkout v<version>` and `make wheel`, and says nothing about the checkout being dirty afterwards. Harmless in itself, and it is the one dirty file `wheel-digest` overwrites with identical content, so the two commands do not compound |
| `LAF-79` | medium | implementing `LAF-64`, branch `fix/curses-install-scope-laf64` | `open` | — `_curses_install_mode` and `_curses_singleselect` still have the two-type shape `LAF-64` removed from `_curses_install_scope`: a return value whose type depends on a flag, where the obvious `isinstance` check compiles, typechecks, and reads a successful selection as a cancel. the `LAF-64` fix removes the shape from that one function; these two are the same trap, untouched by it. Re-checked on `main` `2026-08-16` and **it is one worse than the row says**: `_curses_singleselect` (`tui.py:5424`), `_curses_install_scope` (`:5719`) and `_curses_install_mode` (`:5748`) all take `wizard: bool = False` and all branch their return type on it. Three functions on `main`, two once the `LAF-64` branch merges |
| `LAF-78` | medium | walking `RS-12` live, branch `fix/setup-docker-credentials-rs12` | `open` | — `registry scaffold` cannot scaffold a setup-bearing artifact. Every setup recipe in the constellation was therefore written by hand, which is also why the recipe-format residues (`RS-11`, `RS-13`..`RS-15`) have no scaffolded example to be measured against. Re-checked on `main` `2026-08-16`: `plan_artifact_scaffold` (`registry_commands/planning.py:480`) writes `artifact.json` and one payload file per kind and nothing else, and `ArtifactScaffoldOptions` carries no setup field. The package knows the shape exists — `_adopted_authored`'s docstring lists *a `SETUP.md`, a `setup/` recipe* among what `vendor` adopts from a maintainer who wrote them by hand |
| `LAF-77` | low | implementing `RS-12`, branch `fix/setup-docker-credentials-rs12` | `open` | — the verify probe's environment says it is `_minimal_env` and is not, and after `RS-12` it differs from the adapters it verifies by `DOCKER_CONFIG`. A probe that runs in a different environment from the step it checks can answer for a machine the step never used. Re-checked on `main` `2026-08-16`: `setup_verify_probes._probe_env` allows `PATH`, `LANG`, `LC_ALL`, `LC_CTYPE`, `TERM` **and `HOME`** under a docstring that opens *The environment `setup_runtime._minimal_env` gives a run*; `_minimal_env` allows the first five only. So the first half is true today and the difference is `HOME`; the docstring records why (`PATH` inheritance, measured `2026-08-15`), which is a reason to differ and not a reason to misdescribe. `DOCKER_CONFIG` becomes the second difference when `RS-12` merges |
| `LAF-76` | medium | implementing `RS-12`, branch `fix/setup-docker-credentials-rs12` | `open` | — `custom.install@1` scripts keep the narrow environment `RS-12` widened for the docker adapters. They get `PATH`, `LANG`, `LC_ALL`, `LC_CTYPE` and `TERM` and no `HOME`, so any tool they invoke that reads a dotfile — including `docker` — behaves as it did before `RS-12`. Deliberate for `curl` and `security`, which `HOME` buys nothing; a recipe's own install script is the case that was not decided. Re-checked on `main` `2026-08-16`: `_custom_phase` (`setup_runtime.py:771`) takes its environment from `_minimal_env` at `:803`, and `_minimal_env` (`:158`) allows exactly those five names. **The finding is contingent on `RS-12` merging** — on `main` every adapter is equally narrow, so there is no gap yet; the gap opens the moment `_docker_env` lands |
| `LAF-15` | major | `2.4.0` live acceptance, `LA-R-26` | `open` | — added on the same *Scope* rule as `LAF-03`: `LAF-107` and `LAF-108` refer to it, so it gets a row. `security scan` requires a canonical object envelope — one JSON file of `{schema_version, entries:[…]}` — and **no `aart` command emits one**; the `2026-08-13` walk reached the command only by importing the private `io/object_store.py:_read_candidate` from Python, and once fed it ran correctly. **Re-measured `2026-08-16` on the shipped surface**: the word *envelope* occurs twice in the whole CLI, in `scan`'s help text which consumes one and in the failure message which rejects one, and no command writes one. Still true, unrepaired, and between those two dates no document recorded whether it was open |
| `LAF-03` | medium | `2.4.0` live acceptance, `LA-R-08` | `open` | — added because *Scope* above says an older id gets a row the first time it is referred to again, and `LAF-108` refers to it. `registry_publication.py` defines the SPDX allowlist and the rule *artifact license is absent or not approved*, and nothing in the package imports it, so no runtime path enforces the publication licence policy; `audit` only warns. Recorded on `2026-08-13` in `PROGRESS-live-acceptance.md` as a *question*, with the same evidence a grep gives today. **Re-measured `2026-08-16`: still true** — zero importers inside `agent_artifacts/`, 506 lines, and the module ships in the wheel built at this commit. Between those two dates no document said whether it was open or closed, which is the gap this register exists to remove |

## Corrections this register forced

Writing the table refuted a claim of the design it records, which is the third time in this stream
that a premise did not survive being checked, and the reason the register is worth having.

`DESIGN-readable-receipt.md` §5 says `RS-10` and `LAF-47` — the merge file left behind, emptied —
*become visible to `verify`*. They do not. Both describe an **install** effect: the `.mcp.json` that
`marketplace uninstall` reduces to `{"mcpServers": {}}` and leaves. `plan_verification` reads a
**setup** record, and the one claim it makes for `json.managed-merge@1` is that the path exists — which
is `true` for an emptied file exactly as it is for a full one. Nothing in the shipped response makes
either finding observable, so both are `open` here and not `visible`.

The design sentence is wrong rather than imprecise, and it is left standing with this correction
beside it, because a design edited until it agrees with the code stops being evidence of what was
believed when it was written.

**`LAF-61` was recorded `visible` and the live run took it back.** `RR-3` shipped a probe for orphaned
run directories and this register recorded the finding as observable on the strength of it. `RR-9`
measured the probe against a real leftover directory and found it scanning
`<project_root>/.agent-artifacts/setup-runs` while runs are created under `<data_root>`
(`setup_engine/application.py:457`), so the claim answers `true` without looking — recorded as
`LAF-66`, and `LAF-61` is `open` again.

That correction is the register earning its place. Under the old regime the `visible` claim would have
lived in a release paragraph, agreed with nothing, and been re-discovered a release later as a new
finding. Here it had one row, the row was wrong, and the row changed.

**The release notes were wrong about the release, and the gate did not cover them.** `LAF-74`.
`github-release-v2.6.0.md` still listed `LAF-63` under *known defects shipped open* after `RR-10`
closed it, and told operators to *prefer recipes whose inputs are named without a namespace prefix
until it is fixed* — advice about a defect that is not there, in the text about to be published.
`docs-check` passed, because this list excluded released documents and a release note was assumed to
be one.

It is not, until it is published. Before that moment it is the most current document in the
repository and the only one an operator outside the project will read. Adding it to the list failed
`DOC009` immediately, which is the whole of the fix and the whole of the evidence.

The exclusion below is still right for what it was written for. What was wrong was the word
*released* doing two jobs: naming a document's genre, and asserting a fact about time. The entry
comes out the moment the release is published, and then the original rule applies to it unchanged.

**`LAF-61` is `visible` again, and this time on a different kind of evidence.** `RR-10D` gave the
probe the run root the engine writes into. The claim being made is the same claim `RR-3` made and
lost, so the thing worth recording is what changed about the *measurement*: the first claim rested
on a test that drove a fake probe, which proved the claim rendered and never proved the probe looked
anywhere real. The second rests on `test_laf66_the_probe_reads_the_root_the_engine_writes_into`,
which calls `new_run_directory` — the function a run actually uses — and then the real
`orphan_run_directories`, and then asserts the old location finds nothing, so the fix cannot pass by
widening the search. A claim is only as good as the thing that would falsify it, and the first one
had nothing.

**And the gate did not notice the row changing.** Moving `LAF-61` back to `open` left
`compatibility-v14.md` and `release-checklist-v14.md` both saying `visible`, and `make docs-check`
passed. `DOC009` is one-directional by construction: it fails a document that lists as *shipped open*
something this table records as `closed`, and says nothing about a document claiming `closed` or
`visible` for something this table records as `open` — the direction that asserts a safety which is
not there. Both documents were corrected by hand. That is `LAF-69`, and until it closes, the sentence
above about this file being *enforced* means enforced against stale pessimism only.

**`LAF-69` is closed, and the measurement is the same case run again.** `DOC010` reads the
disposition cell of a checked document and requires it to equal this table. Put `LAF-61` back to
`open` in a copy of these documents and the gate names `compatibility-v14.md:160` — the row that
went a whole release unchallenged. What it does *not* catch is the checklist's sentence, because it
reads cells and not prose; that half is `LAF-83`, recorded rather than quietly counted as fixed.

**And the paragraph at the top of this file overstates the gate.** It says `docs_check` fails when a
document names a finding this file does not carry. No rule does that: a checked document may name
`LAF-99`, claim it `closed`, and every check stays silent, because a lookup that misses returns
nothing and nothing is what it is compared against. That is `LAF-82`. The sentence stays as written
and this correction stands beside it, on the same principle as the design correction above — the
claim is evidence of what was believed, and the row is what says whether it is true.
