{
  "schema_version": 2,
  "protocol_version": 2,
  "artifact": "skill/onboarding",
  "purpose": "Create the reviewed local workspace directory for onboarding notes.",
  "platforms": ["darwin"],
  "help_urls": [],
  "required_tools": [],
  "capabilities": ["custom-code", "process"],
  "inputs": [],
  "custom_entrypoint": "install.sh",
  "steps": [
    {
      "id": "restart",
      "use": "restart.notice@1",
      "with": { "message": "Open a new terminal so the skill sees its workspace." }
    }
  ]
}
